import { stringObject } from './interfaces';

export enum COLORS {
  main = '#FFADAD',
  text = '#4E7E82',
  background = '#FDDBFD',
  border = '#dcdcdc',
}

export enum MEDIA_GATES {
  tablet = '765px',
  desktop = '1100px',
}

export enum SIZES {
  Topbar = '50px',
  item = '40px',
}